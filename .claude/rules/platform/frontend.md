---
paths:
  - "src/**/*.vue"
  - "src/**/*.composable.ts"
  - "src/**/components/**/*.ts"
---

<!-- Generated by aw — do not edit manually -->

# Frontend Rules

> Extends [root AGENTS.md](../../AGENTS.md). Auto-loaded when editing `*.vue`, `composables/*.ts`, `stores/*.ts`.

## Always

- Use Highrise HL components (`HLButton`, `HLInput`, etc.) from `@platform-ui/highrise` — no raw HTML. [MUST]
- Use `v-model:value` (not `v-model`) for HL inputs; `v-model:show` (not `v-model:open`) for modals. [MUST]
- Wrap all user-facing strings in `$t()` or `t()` — no hardcoded English text in templates. [MUST]
- Render rich text, markdown, AI output, and user-generated HTML only through approved sanitizers/renderers. [MUST]
- Read API origins, feature flags, and browser storage through typed config/session helpers. [MUST]
- Use TanStack Query (`useQuery`, `useMutation`) for server state. [SHOULD]
- Use Pinia stores only for UI state that outlives a single component. [SHOULD]
- Use Highrise or CP design tokens (`--primary-*`, `--gray-*`, `--cp-*`, `--hr-*`) — no magic values. [SHOULD]
- Capture frontend errors with `@platform-ui/sentry` (`captureException`). [SHOULD]

## Never

- Never use `v-html` without `DOMPurify.sanitize()` — XSS risk. [MUST]
- Never use raw HTML elements where an HL component exists. [MUST]
- Never hardcode service URLs or store auth tokens/customer data via raw `localStorage` or `sessionStorage`. [MUST]
- Never add feature-level MFA styles targeting `html`, `body`, `#app`, `#__nuxt`, or other host document/app IDs; scope styles to the MFA namespace or component root. [SHOULD]
- Never add broad `eslint-disable`, `@ts-ignore`, `@ts-expect-error`, or `as any` suppressions without a scoped reason. [SHOULD]
- Never import from barrel files that cross layer boundaries. [SHOULD]
- Never store server-fetched data in Pinia — use TanStack Query. [SHOULD]

## Prefer

- Prefer `<HLButton variant="primary">` over custom button styling.
- Prefer composable functions in `composables/` for reusable reactive logic.
- Prefer `defineProps<{ ... }>()` with TypeScript generics over `defineProps([...])`.
- Prefer `useI18n()` inside `<script setup>` for programmatic translations.
- Prefer the matching framework overlay for frontend-specific rules (`vue`, `nuxt`).
- Prefer `defineAsyncComponent(() => import(...))` for non-critical heavy components (charts, modals, editors) loaded on interaction. [SHOULD]
- Prefer a virtual list component (`vue-virtual-scroller`) over full DOM render for lists > 50 items. [SHOULD]

## Verify

- Run `npm run type-check` before marking frontend work complete.
- Confirm no hardcoded English text exists in `<template>` blocks.
- Confirm all HL component imports resolve correctly.
- Confirm rich content and storage/config access go through approved helpers.

## References

- [no-hardcoded-strings](references/no-hardcoded-strings.md) — vue-i18n setup + key naming
- [highrise-design-system](references/highrise-design-system.md) — HL components + tokens
- [mfa-scoped-styles](references/mfa-scoped-styles.md) — keep MFA CSS inside app namespace/component scope
- [state-management](references/state-management.md) — TanStack Query + Pinia patterns
- [frontend-logging](references/frontend-logging.md) — Sentry error capture
- [rich-content-rendering-policy](references/rich-content-rendering-policy.md) — safe markdown, editor, AI, and HTML rendering
- [config-url-and-storage-boundary](references/config-url-and-storage-boundary.md) — typed config and safe browser storage boundaries
- [type-suppression-budget](references/type-suppression-budget.md) — scoped TypeScript and lint suppressions
- [performance-loading](references/performance-loading.md) — dynamic imports, virtual lists, tree-shaking, bundle health
- [architecture-layers](references/architecture-layers.md) — runtime layers, support directories, and where new code goes
- [api-and-service-boundary](references/api-and-service-boundary.md) — routing API calls through composables/stores; stateless services
- [state-placement](references/state-placement.md) — choosing local vs provide/inject vs store; single source of truth
- [props-down-events-up](references/props-down-events-up.md) — unidirectional data flow, no prop mutation, typed provide/inject
- [reactivity-performance](references/reactivity-performance.md) — computed stability, shallowRef, watcher and list render cost
- [declarative-over-imperative](references/declarative-over-imperative.md) — directives and computed over manual DOM and syncing
- [memory-leak-cleanup](references/memory-leak-cleanup.md) — paired teardown for listeners, timers, observers, subscriptions
- [vue-stack](vue/AGENTS.md) — Vue component structure, Composition API, and SFC guidance
- [nuxt-stack](nuxt/AGENTS.md) — Nuxt-specific overlay and future Nuxt rules
